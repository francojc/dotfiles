#!/usr/bin/env bash
# Wrapper for pandoc_watch

SCRIPT_NAME="pdc-watch"; SCRIPT_VERSION="1.0.0"
. "$(dirname "$0")/common.sh"

main(){
  if [[ -x "$(dirname "$0")/pandoc_watch" ]]; then
    "$(dirname "$0")/pandoc_watch" "$@"
  else
    die "pandoc_watch not found"
  fi
}

main "$@"
