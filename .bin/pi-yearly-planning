#!/usr/bin/env bash
set -euo pipefail
source "${HOME}/.bin/pi-cron-common"

LOG_FILE="${LOG_DIR}/yearly-planning.log"

main() {
  echo "--- $(date '+%Y-%m-%d %H:%M:%S') ---"
  pi_run "vision-yearly-planning"
}

main >> "$LOG_FILE" 2>&1
