#!/usr/bin/env bash
set -euo pipefail
source "${HOME}/.bin/pi-cron-common"

LOG_FILE="${LOG_DIR}/weekly-planning.log"

main() {
  run_phase "weekly-planning" pi_run "weekly-planning"
}

if run_job "weekly-planning" "$LOG_FILE" main; then
  exit 0
else
  exit $?
fi
