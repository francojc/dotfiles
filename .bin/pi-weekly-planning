#!/usr/bin/env bash
set -euo pipefail
source "${HOME}/.bin/pi-cron-common"

LOG_FILE="${LOG_DIR}/weekly-planning.log"

main() {
  echo "--- $(date '+%Y-%m-%d %H:%M:%S') ---"
  pi_run "weekly-planning"
}

main >> "$LOG_FILE" 2>&1
