#!/usr/bin/env bash
# Wrapper for vm_sync_tool.sh

SCRIPT_NAME="vm-sync"; SCRIPT_VERSION="1.0.0"
. "$(dirname "$0")/common"

main(){
  if [[ -x "$(dirname "$0")/vm_sync_tool.sh" ]]; then
    "$(dirname "$0")/vm_sync_tool.sh" "$@"
  else
    die "vm_sync_tool.sh not found"
  fi
}

main "$@"
