#!/usr/bin/env bash
# Wrapper for personal-contacts-setup.sh

SCRIPT_NAME="cntc-setup"; SCRIPT_VERSION="1.0.0"
. "$(dirname "$0")/common.sh"

main(){
  if [[ -x "$(dirname "$0")/personal-contacts-setup.sh" ]]; then
    "$(dirname "$0")/personal-contacts-setup.sh" "$@"
  else
    die "personal-contacts-setup.sh not found"
  fi
}

main "$@"
